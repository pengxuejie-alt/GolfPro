# 微信云开发环境 ID（须与当前小程序在云控制台绑定的环境一致）
#
# 重要：开发/真机调试若与正式版使用同一 AppID + 同一环境 ID，则共用同一数据库与同一 openid 下的数据，
# 在开发里创建、修改、删除的球局就是线上数据，不会「自动隔离」。
#
# 建议：
# 1. 在云开发控制台再创建一个「测试用」环境，把云函数、数据库结构同步到测试环境；
# 2. 本机复制本文件为 .env.development，填写测试环境 ID；发版/CI 用 .env.production 填正式环境 ID；
# 3. 或单独注册一个「开发用」小程序 AppID，仅绑定测试云环境（与正式小程序完全隔离）。
#
# 未设置时，db.js 会回退到仓库内写死的默认 ID（与历史行为一致）。
VITE_WX_CLOUD_ENV_ID=cloud1-d8g0wzexa9311e1d5
